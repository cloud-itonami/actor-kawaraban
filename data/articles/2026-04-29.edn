;; kawaraban 瓦版 — live mirror archive（自動生成 / 手編集しない）
;; `kawaraban.store/append-articles!` が追記マージする。1行1記事、
;; :news.article/id で dedupe、as-of 降順。`schema/news.edn` でそのまま
;; transact でき、`clojure -M:query` が読む。
;;
;; G4 — headline + canonical url + 280字以内の excerpt + outlet のみ。
;; 本文は決して含まれない（含む record は ingest が refuse する）。
;; G5 — :news.article/sourcing :verified = その媒体自身の feed から実際に
;; 取得した記事。data/seed.edn の :representative（例示）とは別物。

[#:news.article{:outlet "outlet.granma", :lang ["en"], :kind :mirror, :headline "Signatures for peace, for life", :as-of 1777457046, :url "http://en.granma.cu/cuba/2026-04-29/signatures-for-peace-for-life", :section "sec.international", :byline "Ortelio González Martínez", :excerpt "<p>This act transcends the symbolic: it is a reaffirmation that this land will never renounce its self-determination</p>", :id "art.outlet.granma.1777457046", :sourcing :verified}]
